# Quick Demo (No printer needed)

1. Download `eps-windows-bundle.zip` → extract
2. Open Git Bash in that folder
3. Run `./eps --role ws` (WebSocket)
4. Run `./eps --role http` (Web GUI)
5. Visit **[http://localhost:1100](http://localhost:1100)**, click **Test Printer**
6. Check your CLI for print output!
